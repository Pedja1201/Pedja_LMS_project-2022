import { PomeranjeKursoraDirective } from './pomeranje-kursora.directive';

describe('PomeranjeKursoraDirective', () => {
  it('should create an instance', () => {
    const directive = new PomeranjeKursoraDirective();
    expect(directive).toBeTruthy();
  });
});
