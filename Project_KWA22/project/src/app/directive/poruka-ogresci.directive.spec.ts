import { PorukaOGresciDirective } from './poruka-ogresci.directive';

describe('PorukaOGresciDirective', () => {
  it('should create an instance', () => {
    const directive = new PorukaOGresciDirective();
    expect(directive).toBeTruthy();
  });
});
